# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# Node (skills CLI)
node_modules/

# Duplicate skill copy in a different agent-format layout (same content as
# .agents/skills/, produced by the skills installer for non-Claude-Code
# agents); not used by Claude Code and not something we authored.
/agent/

# Vendored upstream reference repos (own git history; not part of this repo)
reference/

# Secrets — never commit. API keys are read from here via the environment.
.env
.env.*
!.env.example

# Local data caches
.qlib/

# OS
.DS_Store

# Dashboard data snapshot — regenerate with `python -m dashboard.export`
frontend/data/snapshot.json
scripts/logs/
